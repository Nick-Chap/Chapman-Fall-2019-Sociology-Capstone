---
title: "Create Database"
author: "Nick chapman"
date: '(`r format(Sys.time(), "%B %d, %Y")`)'
output: 
  github_document: default
  html_notebook: default 
---

## Introduction
This notebook contains the code to store the Data downloaded from "County Health Rankings Data" for Missouri, and Demographic Data from `tidycensus` into a `SQL` database.

## Dependencies
This notebook requires a few packages:

```{r load-packages}
# tidyverse packages
```

## Load Data
This notebook requires data downloaded, saved to the `data/` folder:

```{r load-data}
```